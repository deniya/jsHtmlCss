1. сохранение данных  (перенаправление) newsAdd.html по newsAdd.js в файл HTML newsList.html по соединению с newsList.js   | |;| |
	по одному ID (т.к. иначе придется прописывать всю таблицу в js, видимо)
2. 

соединение ДС между собой
сохранение\копирование данных -- извлечение\перенос
